[project]
name = "one-rag"
version = "0.1.0"
description = "A minimal RAG retrieval pipeline"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.0,<2",
    "qdrant-client>=1.12,<1.13",
    "sentence-transformers>=3.0,<4",
]
